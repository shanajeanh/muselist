require 'rails_helper'

feature 'user adds character' do
  context 'as a logged-in user' do
    scenario 'there is a list of characters' do

    end

    scenario 'there is a button to add a new character to each canon' do

    end

    scenario 'character can be added with just a name and username' do

    end

    scenario 'character cannot be added without name or username' do

    end

    scenario 'character can also take an optional image url and blurb' do

    end
  end

  context 'as a logged out user' do
    scenario 'there is no button to add a new character' do

    end

    scenario 'navigating directly to the new character page does not work' do
      
    end
  end
end
